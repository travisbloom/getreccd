'use client'

export function ErrorPagesContent(_props: { reset: () => void }) {
  return (
    <div className="container flex flex-grow flex-col items-center justify-center space-y-7">
      TODO ERROR PAGE
    </div>
  )
}
